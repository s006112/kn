version: "3.9"

services:
  db:
    image: mysql:8.0
    container_name: kn-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: led
      MYSQL_USER: led
      MYSQL_PASSWORD: led
    ports:
      - "3306:3306"
