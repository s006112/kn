# ============================================================================
# Core runtime / configuration
# ============================================================================
python-dotenv==1.2.1          # Load environment variables from .env
watchdog==6.0.0              # File system event monitoring
requests==2.32.5             # HTTP client
jinja2==3.1.6                # Template engine
Flask==3.1.2                 # Lightweight web framework
gradio==5.46.0               # Web UI for ML apps
gradio_client==1.13.0        # Python client for Gradio services

# ============================================================================
# LLM API clients / cloud models
# ============================================================================
openai==2.8.1                # OpenAI API SDK (new style client)
perplexityai==0.20.0         # Perplexity API client
google-genai==1.52.0         # Google Gemini / GenAI client

# ============================================================================
# Document parsing / OCR / RAG ingestion
# ============================================================================
pillow==12.1.0 \             # Image processing backend
pytesseract==0.3.13 \        # Tesseract OCR wrapper
bs4==0.0.2                   # BeautifulSoup HTML wrapper
PyMuPDF==1.24.10             # PDF rendering and parsing
ocrmypdf==16.11.0            # OCR pipeline for PDFs, replaced by tesseract
python-docx==0.8.11          # DOCX reader/writer
openpyxl==3.1.5              # XLSX reader/writer
xlrd==2.0.1                  # Legacy XLS reader

# ============================================================================
# Scientific / Numeric stack (P100 compatible)
# ============================================================================
numpy==1.26.2                # Downgraded for faiss-gpu compatibility
scipy==1.15.3                # Scientific computing
opencv_python_headless==4.12.0.88  # OpenCV without GUI

# ============================================================================
# Video / Media utilities
# (installed with --no-deps and --break-system-packages)
# ============================================================================
yt-dlp                       # Latest yt-dlp via pip --no-deps










# ============================================================================
# Audio / Speech
# ============================================================================
sounddevice==0.5.3           # Audio input/output
openai-whisper==20240930     # Whisper ASR (only source tarball available offline)

# ============================================================================
# PyTorch + CUDA (NVIDIA P100 only)
# ============================================================================
torch==2.2.2+cu118           # CUDA 11.8 build; 2.1.x causes pytree/transformer issues

# ============================================================================
# Vector search / Embedding (GPU)
# ============================================================================
faiss-gpu==1.7.2             # GPU FAISS, conflicts with numpy>=2.x
sentence-transformers==5.1.0 # Embedding models wrapper
einops==0.8.1                # Tensor reshaping utilities

# ============================================================================
# HuggingFace / LLM inference toolchain
# ============================================================================
transformers==4.57.1         # Core model loader/inference
accelerate==1.12.0           # Multi-GPU / mixed precision helper
diffusers==0.36.0            # Stable Diffusion / Z-Image pipeline support
kernels==0.11.1              # Jupyter / kernel utilities
protobuf==6.33.1             # HF + gRPC dependency
sentencepiece==0.2.1         # Tokenizer backend
mistral-common==1.8.6        # Required tokenizer/utils for Mistral models

# ============================================================================
# Optional high-performance LLM components
# ============================================================================
bitsandbytes==0.48.2         # 8/4-bit quantization (if CUDA supports)

